import {BlossomSize} from '../../common'

// Avatar
export const SIZE_MAP: Record<BlossomSize, number> = {
  small: 40,
  medium: 64,
  large: 96,
}

export const OFFSET = 8

// AvatarGroup

export const SPACING_MAP: Record<BlossomSize, number> = {
  small: -10,
  medium: -16,
  large: -20,
}
